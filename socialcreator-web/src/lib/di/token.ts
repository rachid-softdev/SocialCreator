/**
 * DI Service Tokens (deprecated)
 *
 * These tokens were part of the Container DI system that was never adopted.
 * The codebase uses the repository registry pattern (getRepositories()) instead.
 *
 * @deprecated Use repository types from "@/lib/repositories" instead.
 */
